# PM-5 — Establish lead/satellite coordination contracts

## Objective

Give the Project Map a coordination protocol: one lead orchestrator per repository, satellites that claim one capability each, shared contracts that are proposed and decided with durable evidence, and a read-only projection that reports who is where, what conflicts, and what the next safe action is. The protocol carries no delivery authority and never writes the versioned map outside one bounded field.

## Problem

PM-4 built the durable store: claims, leases, heartbeats, session bindings, blockers and readiness receipts are files under the canonical Git common directory, with a lock, a generation tuple and bounded history. What does not exist yet is the protocol that uses it. Today a satellite can claim a capability, but nothing says who the lead is, nothing records a shared-contract proposal, nothing detects a conflicting claim or an unavailable peer, and the versioned map's `contracts` field is a list of unresolvable strings — PM-1 recorded that the shared registry "arrives with PM-5".

## Why

The roadmap's authority model splits coordination from delivery: the lead owns the canonical map, contested boundaries and shared contracts, satellites propose but cannot rewrite another capability's scope, and the human approves consequential decisions. A store without a protocol leaves every one of those sentences unenforced and unobservable: the Project Map card cannot show an owner, a contract state or a next action, and PM-6 (worktrees), PM-7 (launch) and PM-8 (integration readiness) all consume a protocol that does not exist yet.

## Decisions (approved by the user, 2026-09-25)

| Question | Options offered | Chosen |
|---|---|---|
| Where does a shared-contract proposal live and what shape does it have? | a new `contract-proposal` store record kind; notification only, no durable record; one record per capability with an embedded decision history | **A new `contract-proposal` record kind in the store** |
| What may the lead write when it accepts a shared contract? | write the map directly; register in the store and let the human apply it | **Write the map directly — scoped to the `contracts` array of an already approved map** (confirmed in a follow-up question, after the conflict with PM-2 and the roadmap was spelled out: capabilities, surfaces, dependencies, foundations and the approval state stay human-gated, and every write is mirrored as durable evidence in the store) |
| Which events are durable records and which are derived or signals? | durable = state transitions, the rest derived; a durable journal of every event; a journal only for contract decisions | **Durable = state transitions; `dependency-ready` and `completion` are derived; notifications are signals** |
| How is the lead identified in the store and how is split-brain avoided? | a claim on a reserved capability id; a dedicated `leader` record; the descriptor epoch owner | **A claim on a reserved capability id** |

The user's second answer is a deliberate change to the authority model the roadmap sketched, and it is recorded as such: the lead gains a bounded write path into the versioned artifact. Everything else in the map — the capability list, their surfaces, dependencies and foundations, and the `draft`/`approved` state — remains the human's, and PM-2's refusal to re-approve an approved map is untouched.

## Scope

### Included

- The `contract-proposal` record kind: propose, decide (accept or reject), read and list, with durable evidence and no overwriting of a decided proposal.
- The lead as a claim on a reserved capability id, with the lease semantics PM-4 already implements (a live lease is never stolen, a stale one is recovered with a visible warning).
- The bounded map write: an accepted contract is applied to the `contracts` array of an already approved map, and nothing else in the artifact moves.
- A read-only coordination projection: lead state, satellite claims, conflicts (unknown capability, undeclared surfaces, unavailable peer, reserved id declared, stale generation), dependency readiness, and the next safe action.
- The smallest command surface that makes the protocol usable by a human and a satellite: lead claim/renew/release/status, and contract propose/accept/reject/list.

### Non-goals

- Worktree provisioning, branch derivation, dirty-state inspection and cleanup (PM-6).
- Opening Pi in a capability worktree, terminal adapters and the handoff payload (PM-7).
- Integration ordering and merge-conflict prediction (PM-8).
- Rendering the coordination state in the Project Map card: PM-5 produces the projection the card will consume, and wiring it into the card belongs to the slice that renders it.
- Any delivery authority: no commit, push, PR, merge or release path, and no readiness receipt that grants one.
- Any write to the versioned map outside the `contracts` array of one capability.
- A durable event journal: the store records are the state, and a second journal would be a second source of truth (the same reasoning PM4-1 used to refuse a standalone lease record).

## Constraints

- TDD mode: strict (`openspec/config.yaml` declares `strict_tdd: true`); runner `node --experimental-strip-types --test tests/<file>.test.ts`.
- Repository style: ESM `.ts` imports with explicit extensions, tabs, double quotes, semicolons. No new runtime dependency.
- Reuse rather than reinvent: the store's lock, readiness gate, digest-not-segment path discipline, canonical serialization and diagnostic vocabulary (`lib/project-map-store-*.ts`); the single authorized map writer `writeProjectMapFile` and the map validator (`lib/shell-project-map-approval.ts`, `lib/shell-project-map-schema.ts`); the read-only projection precedent of `lib/sdd-status.ts`.
- The store records state and does not arbitrate: the store never checks who the lead is. Arbitration ("only the lead decides") lives in the projection and the command layer, where it is testable and visible.
- Runtime fields never enter the versioned map: `PROJECT_MAP_RUNTIME_FIELDS` stays as it is, and applying a contract writes only into the capability's `contracts` array.
- Single writer per worktree; this unit is developed on `feat/project-map-orchestration`.

## Authorized edit surfaces

Slice by slice; no slice may touch a path outside its own list.

- PM5-1 (contract proposals in the store): `lib/project-map-store-contracts.ts` (new), `tests/project-map-store-contracts.test.ts` (new), `lib/project-map-store-schema.ts` for the `contract-proposal` kind and the three contract diagnostic codes only, `tests/project-map-store-schema.test.ts` for the pinned vocabulary and the new record shape, and `lib/project-map-store.ts` plus `tests/project-map-store.test.ts` for adding `contracts` to the record-directory accounting in the emptiness proof (added to this slice after the writer was launched: PM4-5a added `receipts` to that list before the directory existed, and a new record directory the proof cannot see would reproduce the gap that slice closed)
- PM5-2 (the bounded map write): `lib/shell-project-map-contracts.ts` (new), `tests/shell-project-map-contracts.test.ts` (new)
- PM5-3 (coordination projection): `lib/project-map-coordination-state.ts` (new), `tests/project-map-coordination-state.test.ts` (new)
- PM5-4 (command surface): `extensions/gentle-project-map.ts`, `tests/gentle-project-map.test.ts`, and `tests/gentle-project-map-contracts.test.ts` (new) if the slice is split
- PM5-5 (documentation and verification): this document and `odd/tasks/project-map-orchestration.md`
- This document and `odd/tasks/project-map-orchestration.md` for bookkeeping at any point

## Task list

- [x] **PM5-1 — Contract proposals in the store**
  - Design, fixed before the source write (2026-09-25):
    - **Layout**: `contracts/<sha256(capability_id)>/<sha256(contract_id)>.json`, the same digest-not-segment discipline as claims and blockers, one directory level per capability so a capability's contracts stay together.
    - **Record shape, one new kind in the store vocabulary**: `{ schema, kind: "contract-proposal", capability_id, contract_id, title, digest, proposed_by, proposed_at, state, decided_by?, decided_at?, rationale? }` where `state` is `"proposed" | "accepted" | "rejected"`. `title` is a non-empty human label, `digest` is a `sha256:` digest of the contract body — the body itself stays out of the store, because a store that carries documents stops being a store — and `proposed_by`/`decided_by` are session ids.
    - **The state pairing rule mirrors the blocker's**: `state` other than `"proposed"` requires `decided_by`, `decided_at` and `rationale`, and `state: "proposed"` forbids them, so a record can never claim a decision it does not carry. The paired absence is reported as `missing-field` at the absent field's path, exactly as the blocker pairing does.
    - **`superseded` was dropped from the shape the user approved, and this is a recorded refinement**: a decided record is never rewritten, so a revision is a new `contract_id` (for example `billing-v2`) and the map cleans up the old id through an explicit `supersedes` argument on the apply operation. Keeping a `superseded` state would have meant rewriting a decided record — the one thing this store never does.
    - **Operations**, mirroring the blockers module's structure (a private readiness gate, a lock wrapper, functions that never throw):
      - `proposeProjectMapContract({ root, capabilityId, contractId, title, digest, sessionId, now })` refuses an invalid `now` and a store that is not `ready`, runs under the store lock, refuses with `contract-exists` when a valid record already exists and leaves the bytes untouched, and otherwise writes the canonical record with `state: "proposed"`. A corrupt or unreadable file at that path is reported as corruption instead of `contract-exists`, because a damaged file is not an existing contract (the wording here was broader than the implementation until the independent audit flagged it; the implementation's behaviour is the correct one and is now what this line says).
      - `decideProjectMapContract({ root, capabilityId, contractId, decision, rationale, sessionId, now })` refuses `contract-absent` when there is no file, `contract-already-decided` when a decision is already recorded — a decision is evidence and a second one may not overwrite the first — and otherwise rewrites the record with `state`, `decided_by`, `decided_at` and `rationale`, keeping every other field identical.
      - `readProjectMapContract({ root, capabilityId, contractId })` is lock-free and returns `{ contract, status, diagnostics }` with `free`, `proposed`, `accepted`, `rejected`, `corrupted` or `unreadable`; the status comes from the recorded state, never from comparing instants. Canonical bytes and both declared ids are verified against the requested pair, and a mismatch is `store-corrupted` at the offending path.
      - `listProjectMapContracts({ root, capabilityId, includeDecided })` enumerates only that capability's directory, sorts chronologically by `Date.parse(proposed_at)` with a code-unit `contract_id` tie-break — never `localeCompare`, the defect this unit fixed twice — excludes corrupt entries and reports them as `store-corrupted`.
    - **Three new diagnostic codes**: `contract-exists`, `contract-absent` and `contract-already-decided`. Unparsable, non-canonical or mismatched records reuse `unreadable-store` and `store-corrupted`; no existing code changes meaning.
    - **The store does not arbitrate who may decide**: `decideProjectMapContract` records the deciding session but never checks it against the lead. Enforcing "only the lead decides" is PM5-3's and PM5-4's job, where the lead claim is visible.
    - **The emptiness proof learns about the new directory in this slice**: `PROJECT_MAP_STORE_RECORD_DIRECTORIES` in `lib/project-map-store.ts` gains `"contracts"`, with the expectation in `tests/project-map-store.test.ts` that a store holding a contract is not provably empty and that the `store-not-empty` diagnostic names the directory. PM4-5a added `receipts` there before its directory existed for exactly this reason: a record directory the proof cannot see is a directory that can be initialized over.
    - Tests, written first and watched fail: two contracts proposed for one capability and both readable by id; a second proposal of the same id refused with `contract-exists` and byte-identical storage; an accept that records state, decider, instant and rationale while keeping the proposal's own fields; a reject that does the same with the other state; a second decision refused with `contract-already-decided` and the first decision preserved byte-for-byte; a decision on an absent contract reporting `contract-absent`; a list that honours `includeDecided` and orders chronologically across mixed ISO offsets; a valid-but-non-canonical record, a mismatched capability id and a mismatched contract id each classified `corrupted`; a decision missing its rationale classified `corrupted` through the pairing rule; every operation on an uninitialized store refused with nothing written; and an adversarial capability id and contract id (`../` segments) landing inside `contracts/` with nothing outside it.
    - Evidence expectation: RED before GREEN, mutation probes on the exists-refusal, the already-decided refusal and the mismatch classifications, the four gates (focused, neighbours, full suite, `check-types`), independent verification and one native review.
  - Evidence for PM5-1: commit `e69cea04` (6 files, 574 insertions / 6 deletions) plus `9580a63d`, which closed the coverage gaps the independent audit named (4 files, 174/13). Verified by the parent; RDD is off by the user's decision, so no lineage was opened.
  - **Measured size, reported rather than hidden**: 574 additions against the 400-line review budget and the 350-line forecast, 43% over budget. The tests are 259 lines and the module 268, and no cohesive cut exists: `propose` and `decide` share the lock wrapper and the classifier, and separating `list` would be a ~25-line slice that duplicates the scaffolding.
  - RED and sensitivity: against `75105dff` the contract tests fail 0/13, each on its own `assert.ok(module, ...)` guard rather than as a file-load error, and schema plus store 38/41 with exactly the three amended expectations failing. Twelve parent-run mutation probes (copies under `/tmp/pm51-verify/`), each killed by exactly the intended test: removing the `contract-exists` refusal; removing the `contract-already-decided` refusal; reverting the canonical comparison to decoded text; removing the capability-id mismatch check; removing the contract-id mismatch check; switching the tie-break to `localeCompare`; removing the `.tmp` filter; using raw ids as path segments; dropping `contracts` from the emptiness list; and disabling each of the three pairing rules.
  - Gates on the final tree: focused 54/54, the whole project-map store family 129/129, full suite 3708 / 3668 pass / 0 fail / 38 skipped (measured after the disk-pressure incident below), and `node scripts/check-types.mjs` with 195 recorded diagnostics and no regressions.
  - **One defect the parent found before the first commit**: the new module reproduced the weakness the receipts review had already found — the canonical comparison was over decoded text, so an invalid UTF-8 byte that decodes to the same character passed. It now compares raw bytes, with a test that discriminates.
  - **The independent audit found no production defect and six test-quality gaps, all closed in `9580a63d`**: the missing-rationale fixture was also non-canonical, so the pairing rule was not isolated (the bytes are now built by hand so the rule is the only rejection reason); the emptiness test populated four directories at once, so ignoring `contracts` in the decision while keeping it in the diagnostics still passed (there is now a contract-only case); "preserving every proposal field" used the fixture defaults, so a decision that reset the metadata passed (the metadata is now deliberately varied); and lock contention, a corrupt descriptor, invalid mutation inputs, immutable terminal states, list isolation with foreign and corrupt entries, the `free`/`unreadable` read statuses and the no-arbitration behaviour had no coverage at all. Six further mutation probes confirm each new assertion discriminates.
  - **One specification discrepancy the audit flagged, corrected in the document rather than in the code**: this block said "refuses with `contract-exists` when the file already exists", but a corrupt or unreadable file is reported as corruption. The implementation is right — a damaged file is not an existing contract — and the design line now says what the code does.

- [x] **PM5-2 — Apply an accepted contract to the approved map**
  - Design, fixed before the source write (2026-09-25):
    - **One bounded writer, one bounded field**: `lib/shell-project-map-contracts.ts` exports `applyProjectMapContract({ path, capabilityId, contractId, supersedes? })`, which reads `openspec/project-map.json`, requires `approval.state === "approved"`, requires the capability to exist, adds `contractId` to that capability's `contracts` array, optionally removes `supersedes`, re-validates the whole map with `validateProjectMap` and writes it through `writeProjectMapFile` — the single authorized writer. Nothing else in the artifact is touched: not the approval block, not capabilities, surfaces, dependencies or foundations, and no runtime field is ever added.
    - **The byte-identity claim carries a precondition the independent audit made explicit**: `writeProjectMapFile` serializes through `canonicalizeProjectMap`, which sorts foundations, capabilities and every capability array (`foundationRefs`, `dependsOn`, `contracts`, `featureDocs`, `surfaces`) and their `evidence`. So "every other byte identical" holds for an artifact that is already canonical — which is the only kind the authorized writers produce, because PM-2's `declare` and `approve` write through the same function — while a valid but non-canonical artifact is normalized by the write. That is the writer's documented behaviour and not a field-level edit, and the first version of this block stated the claim without its precondition.
    - **Why this exists at all**: the user's decision gives the lead a direct write path for shared contracts, and the roadmap's own sentence assigns the lead ownership of the canonical map and shared contracts. Scoping it to `contracts` is what keeps the human's approval the gate for every boundary decision.
    - **A draft map is refused**: contracts are applied to an approved map only, because on a draft the human is still authoring and PM-2's `declare` and `approve` are the operations that matter. The refusal reuses the map vocabulary's `invalid-field` at `$.approval.state` rather than inventing a store code, since this is not a store record.
    - **Idempotent and honest about it**: applying an id that is already present writes nothing and returns `applied: false` — unless a `supersedes` removal also changes the array, in which case the write happens and `removed: true` says so (the independent audit caught the unconditional version of this sentence, which was wrong). A `supersedes` id that is not present returns `removed: false`. Both are visible in the result instead of being silent no-ops, and the return carries the resulting map so the caller can report it.
    - **Deterministic artifact**: `supersedes` is removed wherever it appears and an additive set applied in any order leaves the bytes identical, because `validateProjectMap` canonicalizes the map it returns and `writeProjectMapFile` serializes that canonical form — so the array is sorted rather than kept in insertion order, and no caller can observe the intermediate candidate. Determinism is scoped to additive sets on purpose: a supersession sequence is a different set of operations, not a reordering of the same one, so `add alpha` then `add beta superseding alpha` is not the same request as its reverse and the two are not expected to agree. A test pins the determinism and the normalization.
    - **The caller is responsible for the evidence**: applying a contract is not a store operation, so this module does not write a record. The command layer (PM5-4) applies a contract only after the store holds its accepted decision, which is what makes the pair auditable.
    - Tests, written first and watched fail: applying an accepted id to an approved map adds exactly that id and leaves every other byte of the artifact identical; a second apply is a no-op reporting `applied: false` with identical bytes; `supersedes` removes the old id and adds the new one in one write; a draft map is refused and the file is untouched; an unknown capability is refused; an empty or whitespace-only contract id is refused; a map that fails validation is never written; and applying to a missing or corrupt artifact fails closed without creating anything.
    - Evidence expectation: RED before GREEN, mutation probes on the approval gate, the `supersedes` removal and the no-op path, the four gates, independent verification and one native review.
  - Evidence for PM5-2: commit `22ee3815` (2 files, 320 insertions). Verified by the parent; RDD is off, so no lineage was opened.
  - **Measured size**: 320 additions against the 400-line review budget, under it, though 45% over the 220-line forecast because the tests are 246 lines for ten byte-level cases. The two audit-closure commits that followed added 135 more lines to the test file (it now holds sixteen tests), so the slice's final size is 455 additions and it is recorded as over budget rather than hidden.
  - RED and sensitivity: against the pre-change tree the ten tests that existed then fail 0/10, each on its own `applyProjectMapContract must be exported` guard rather than as a file-load error (the file now holds sixteen tests, and the six added later were verified by their own mutation probes rather than by that RED run). Eight parent-run mutation probes (copies under `/tmp/pm52-verify/`): removing the approval gate, the capability lookup, the empty-id check, the `supersedes` filter, the capability index (writing into the wrong capability) and the `supersedes` presence check each die on the intended test; and one survives by design — removing the pre-write validation — because `writeProjectMapFile` validates before writing, so that layer is defensive rather than load-bearing. **The other survivor the first round reported was a wrong conclusion and the second audit corrected it**: removing the no-op early return is NOT behaviour-preserving, because without it a no-op canonicalizes and rewrites a valid but non-canonical artifact, which is exactly what a no-op must not do. The first probe missed it because its fixture was canonical and the atomic writer skips identical bytes; a test now pins the non-canonical no-op and the mutant dies on it, together with the self-supersession and absent-supersession cases that also return through the early return.
  - **One design promise the writer left untested and the parent added**: a `supersedes` id that is not present must report `removed: false` without inventing a removal; the new test is what kills the mutation that drops that check.
  - **A nuance the artifact's canonicalization introduces, recorded because the design wording was imprecise and the independent audit caught it**: `validateProjectMap` itself returns `canonicalizeProjectMap(...)`, and `writeProjectMapFile` serializes through the same canonical form. So the returned map and the written artifact are both canonical — foundations, capabilities and every capability array sorted — and the design's "the array keeps its existing order and appends" describes an intermediate candidate that no caller can observe. The real guarantee is determinism for additive sets: applying `{alpha, beta}` in either order yields identical bytes, while a supersession sequence is a different request and is not expected to agree with its reverse. The byte-identity claim therefore carries the precondition the audit made explicit, now stated in the design block and in the acceptance criteria.
  - **Three further audit findings recorded rather than papered over**: the pre-write validation cannot be exercised through ordinary valid request inputs (adding an absent id and removing a present one cannot invalidate a map that was valid, so that layer is defensive and the writer's own validation is what a test can reach — it does not mean the validator never runs); "one write" is not measured by these tests rather than inherently unmeasurable, because writer or filesystem instrumentation could count replacements, and the replacement test would pass an implementation doing several successful intermediate writes; and the per-test export guard that makes RED per-test cannot distinguish an absent module from a syntax or dependency error, so for a new module the absence itself is the evidence and the probe should say so.
  - **Where the order claim is bounded**: returned maps and written artifacts are always canonical, while a file that a no-op did not touch keeps whatever order it had — so "order preservation is not observable" is true of everything a caller receives, not of every artifact on disk.
  - Gates on the final tree: focused 16/16, the neighbouring map suites 96/96, full suite 3728 / 3690 pass / 0 fail / 38 skipped, and `node scripts/check-types.mjs` with no regressions. The three probes re-run after the audit closure (the no-op early return, the self-supersession path and the absent-supersession path) now die on the intended tests.
  - **An operational incident worth recording, because it produced a false failure report**: the full suite showed two failures and then timed out while PM5-2 was still in flight, and the parent attributed them to the concurrent writer's RED tests. That attribution was wrong. `/tmp` is a 16 GB tmpfs and the parent's accumulated mutation-probe copies from every slice of PM-4 and PM-5 had filled it to 80% (13 GB), so the tests were failing with `errno -122` (`EDQUOT`, disk quota exceeded) inside their own temp directories. Cleaning the probe copies restored the suite to green and the writer's report to being accurate. The lesson is in the workflow, not in the code: probe directories are deleted as soon as their slice is verified.

- [ ] **PM5-3 — Coordination projection**
  - Design, fixed before the source write (2026-09-25):
    - **Read-only, no authority, one exported reader**: `lib/project-map-coordination-state.ts` exports `PROJECT_MAP_LEAD_CAPABILITY_ID = "__lead"` and `readProjectMapCoordinationState({ root, mapPath, now, expectedGeneration? })`, which returns a projection and never writes. It mirrors `lib/sdd-status.ts`: the projection reports ground truth, and no caller may fabricate readiness from it.
    - **The lead is a claim, not a new record kind**: the projection reads the claim on the reserved id and reports `{ sessionId, lease: { renewal_after, renew_by }, status: "free" | "live" | "stale" }`. Split-brain is impossible while a lease is live (PM4-3 refuses to steal one), and a dead lead is recovered after 60 seconds with the `stale-claim-recovered` warning PM4-3 already emits, which the projection surfaces as a recorded fact rather than a silent takeover.
    - **Satellites**: every claim in `claims/` is projected with its capability, session and lease state. A claim whose capability is not declared in the map is a `unknown-capability` conflict; a claim on the reserved lead id is never reported as a satellite.
    - **The conflicts it can honestly detect, and the one it cannot**: `unknown-capability` (a claim the map does not declare), `undeclared-surfaces` (a claim on a capability whose surface list is empty — the advisory scope signal, because file-level enforcement needs PM-6's worktree-to-capability binding and pretending otherwise would be a lie), `unavailable-peer` (a live claim whose session has no fresh heartbeat), `reserved-capability-declared` (the map declaring `__lead` as a capability, which would make the reserved id ambiguous), and `stale-generation` (when the caller passes `expectedGeneration` and the descriptor has moved on). Each conflict carries the evidence that produced it: the capability id, the session id and the instant.
    - **`dependency-ready` and `completion` are derived, never stored**: a capability is dependency-ready when every id in its `dependsOn` exists in the map and is not `blocked` and has at least one readiness receipt; a capability is complete when it has a readiness receipt. Both are computed in the projection from the map plus `receipts/`, which is exactly the user's decision: durable state transitions are records, everything else is derived.
    - **Next safe action, bounded and never a delivery action**: each capability gets one of `claim`, `wait-for-dependency`, `resolve-blocker`, `decide-contract`, `integrate`, `done` or `blocked`, chosen from the projected facts in a documented order, and the projection never suggests commit, push, PR or merge.
    - **A corrupt store is a refusal, not an empty projection**: when the descriptor is not `ready` or a record is corrupt, the projection returns its diagnostics and marks the affected part unknown rather than reporting "no claims" — the fail-closed rule this store already follows.
    - **A parent refinement before the source write, so the slice has no room to invent shape**: the projection returns `{ map, lead, satellites, conflicts, capabilities, diagnostics }`; `lead` is `{ capabilityId, sessionId, lease, status }` with status `free | live | stale | corrupted`; each satellite is `{ capabilityId, sessionId, lease, status: live | stale, heartbeat: fresh | stale | missing | corrupted }`; each conflict is `{ code, capabilityId?, sessionId?, message }` with the five codes named above; and each capability is `{ capabilityId, dependencyReady, complete, openBlockers, proposedContracts, nextSafeAction }`. The next-safe-action order is fixed and documented: `done` (the map says done), `blocked` (the map says blocked), `resolve-blocker` (an open blocker), `wait-for-dependency` (not dependency-ready), `decide-contract` (a proposed contract awaits a decision), `integrate` (a readiness receipt exists), `work` (claimed, dependency-ready and clear — the one action added to the list this block originally gave, because a claimed capability that is simply in progress had no name), and `claim` (no live claim). It never suggests commit, push, PR or merge.
    - **Satellites are enumerated from the store, not from the map**: the claim module reads one claim by capability id, so a claim on a capability the map does not declare would be invisible if the projection iterated the map. The projection therefore lists `claims/` itself (skipping `.tmp` names, verifying canonical bytes exactly as the store modules do) and reports a claim whose capability is absent from the map as the `unknown-capability` conflict.
    - Tests, written first and watched fail: a free store projects no lead and no satellites; a live lead claim projects its session and lease; a stale lead claim is reported as stale with its previous holder named; two capabilities claimed by two sessions both appear; an unknown capability claim is a conflict; a claim on a capability with empty surfaces is an `undeclared-surfaces` conflict; a claim whose session has no heartbeat is `unavailable-peer`; a map declaring `__lead` is `reserved-capability-declared`; a moved generation is `stale-generation` when expected; dependency readiness follows the map's `dependsOn` states plus receipts; completion follows a readiness receipt; every next-safe-action branch is reached by a fixture; a corrupt descriptor or corrupt claim yields diagnostics and unknown parts instead of an empty projection; and the projection writes nothing (the store bytes before and after are identical).
    - Evidence expectation: RED before GREEN, mutation probes on the lead resolution, the conflict branches and the fail-closed path, the four gates, independent verification and one native review.
  - Evidence for PM5-3: commit `5a889daf` (2 files, 525 insertions). Verified by the parent; RDD is off by the user's decision, so no lineage was opened.
  - **Measured size**: 525 additions against the 400-line review budget and the 350-line forecast, 31% over budget. The tests are 303 lines for fourteen fixtures and the module 222. No cohesive cut exists: every conflict branch shares the same satellite enumeration and the same capability projection, and separating conflict detection from the projection would deliver a projection that cannot report what the design asks it to detect.
  - RED and sensitivity: against the pre-change tree the fourteen tests fail 0/14, each on its own `readProjectMapCoordinationState must be exported` guard rather than as a file-load error. Nine parent-run mutation probes (declared as parent-produced, in copies under `/tmp/pm53-verify/`), each killed by exactly the intended test: reporting the lead as a satellite; removing the unknown-capability conflict; removing the undeclared-surfaces conflict; removing the unavailable-peer conflict; removing the reserved-capability-declared conflict; removing the stale-generation conflict; dropping the receipt requirement from dependency readiness; forcing `complete` to false; and changing the claimed-capability action from `work` to `claim`.
  - **One defect the parent found and fixed before the commit**: an unparseable `now` was reported as a diagnostic and then used anyway, which made every live lease look stale — a wrong fact presented as data. The time-dependent parts are now refused with the same unknown shape the projection uses elsewhere, and the new test is what kills the mutation that removes that refusal.
  - **One design promise that had no test and now has one**: the reserved lead id is never reported as a satellite. Removing the exclusion survived the whole matrix until the lead test asserted that the satellite list is empty.
  - Gates on the final tree: focused 14/14, the project-map store family 143/143, full suite 3729 / 3691 pass / 0 fail / 38 skipped, and `node scripts/check-types.mjs` with no regressions.
  - **Recorded, not fixed**: when the descriptor is not ready the projection returns the readable map with empty `satellites`, `conflicts` and `capabilities` arrays plus diagnostics, so a consumer that ignores diagnostics cannot tell "unknown" from "none" — the pinned shape has no nullable array. And `mapDeclaresReservedCapability` re-reads and raw-parses the artifact instead of using the validated map, which reports the conflict even for a map that fails validation; both are deliberate enough to record rather than change blind.

- [ ] **PM5-4 — Command surface**
  - Design, fixed before the source write (2026-09-25):
    - **The smallest wiring that makes the protocol usable**: sub-actions on the existing `/gentle:project-map` command — `lead claim|renew|release|status` and `contract propose|accept|reject|list`. No new command, no new rendering: the card and inspector keep rendering what they render today.
    - **`lead claim` is the only way to become the lead**: it acquires the claim on `PROJECT_MAP_LEAD_CAPABILITY_ID` for the current session, `renew` refreshes it on the PM4-3 cadence, `release` gives it up, and `status` reads the projection. A second session's `claim` is refused with `claim-held` and says so in the user's words.
    - **Only the lead decides, and the command enforces it**: `contract accept` and `contract reject` first check the projection's lead state, require a live lead claim whose session is the caller's, and refuse with a clear message otherwise. This is the arbitration the store deliberately does not do.
    - **`contract accept` writes the map only after the decision is durable**: it records the accepted decision in the store first and applies it to the approved map second, so a failed map write leaves the durable decision intact and the user can retry the apply. `contract propose` takes the contract body's path and computes the `sha256:` digest of its bytes, so the human never types a digest.
    - **Refusals are the store's own codes**: the command reports `contract-exists`, `contract-absent`, `contract-already-decided`, `claim-held` and the map refusals by name, without inventing a parallel vocabulary.
    - Tests, written first and watched fail: a lead claim from one session and its refusal from another; accept refused when the caller is not the lead; accept recorded in the store and applied to the map; a rejected proposal never touches the map; propose refusing a missing contract body file; list showing proposed and decided contracts; release making the lead free; and every refusal surfaced with its store code.
    - Evidence expectation: RED before GREEN, mutation probes on the lead gate and the apply-after-decision order, the four gates, independent verification and one native review. If the slice measures over the review budget it is split into `lead` and `contract` halves before it is committed.
  - Evidence for PM5-4: commit `a1a02a07` (3 files, 479 insertions / 5 deletions). Verified by the parent; RDD is off by the user's decision, so no lineage was opened.
  - **Measured size and a deliberate deviation from this block's own rule**: 484 changed lines against the 400-line review budget, 21% over, and the writer itself flagged it and recommended the split. The split was evaluated and rejected: the halves are not independent. The lead gate exists *for* the contract decisions, so a lead-only first commit would carry a gate with no caller — dead code a reviewer would rightly question — and the contract half would depend on the first. The dispatcher, the session identity helper and the report shape are shared by both halves anyway. This is the same reasoning PM4-3 and PM4-5b used to report overage instead of splitting, and it is written here because this block had pre-committed to the split.
  - RED and sensitivity: against the pre-change tree the new suite fails 0/14 and the existing suite fails exactly one amended expectation (the sub-action list). Eight parent-run mutation probes (declared as parent-produced, in copies under `/tmp/pm54-verify/`), each killed by exactly the intended test: removing the lead gate; removing the confirmation gate; removing the durable-decision guard; forcing `wrote: true`; hashing the body path instead of the body bytes; listing only the first capability; wiring `renew` to the acquisition path; and dropping the two new sub-actions from the list.
  - **Three coverage gaps the parent closed before the commit**, none of which the frozen design's test list had asked for: a declined confirmation must write neither the durable decision nor the artifact (the confirmation is a write gate); an acceptance with no durable proposal must never reach the artifact (the map is never written without a decision, and that guard is what the mutation probe now kills); and an acceptance whose contract is already in the map must report `wrote: false`, which is what pins the honesty of that flag.
  - Gates on the final tree: focused 49/49, the project-map family 341/341, full suite 3743 / 3705 pass / 0 fail / 38 skipped, and `node scripts/check-types.mjs` with no regressions.
  - **Recorded, not fixed**: a contract title is a single whitespace-delimited token, so a title containing spaces is refused with a usage message rather than accepted (the rationale is the last argument and does accept spaces, so the two differ); the confirmation happens before the durable decision, so an acceptance that is doomed to fail still asks the human first; and `bodyDigest` reports `project-map-store/unreadable-store` as a string literal instead of the vocabulary constant the store modules use.
  - **The writer's own disclosures, recorded because a report is not evidence**: its first RED run had a fixture `EEXIST` error in the map-apply test, so that single test did not provide valid pre-implementation RED evidence; and the apply-failure test reaches the failure by replacing the artifact with a directory rather than by removing write permission from its directory.

- [x] **PM5-5 — Documentation and verification**
  - Document the protocol where a human meets it: the roadmap pointer, the command usage, what a satellite may and may not do, and how the lead's bounded write path interacts with the human's approval.
  - Verify the unit: the acceptance criteria below, the full suite, the type gate, and a written record of what was verified versus what was not.

  - Evidence for PM5-5: commits for the documentation and the stale-string fix, and the acceptance-criteria verification below. This slice was done by the parent rather than delegated: it is documentation plus verification, and verification is the parent's own job. The surfaces were extended for it, and the extension is recorded: `docs/project-map.md` and `docs/gentle-shell.md` were added because the protocol has to be documented where a human meets it, and `lib/shell-project-map-view.ts` plus `tests/shell-project-map-view.test.ts` were added for one stale string.
  - **A product string that PM-4 had made false, fixed here**: the card's Inspector rendered `Runtime overlay: unavailable until PM-4.` and PM-4 is delivered. It now reads `Runtime overlay: not wired yet.`, which is the truth: the coordination store and the projection exist, and the card does not consume them yet. Wiring the overlay into the card remains a separate slice, and the design's non-goal still stands.
  - **Acceptance criteria, each one traced to the evidence that pins it**:
    - A proposal is a canonical durable record with a body digest and a decided proposal is never overwritten — `tests/project-map-store-contracts.test.ts` (duplicate proposal, second decision, canonical bytes on disk).
    - A second decision is refused and the first survives byte-for-byte — the same file, plus the parent-added immutable-terminal-states test.
    - The lead is a claim on the reserved id: a second lead is refused while a lease is live and a stale one is recovered with a visible warning — `tests/project-map-store-claims.test.ts` for the lease semantics and `tests/gentle-project-map-contracts.test.ts` for the command's `claim-held`.
    - Only the session holding the lead claim may decide, and the check lives in the command and the projection — `tests/gentle-project-map-contracts.test.ts` (the non-lead refusal) and `tests/project-map-coordination-state.test.ts` (the projection's lead state).
    - An accepted contract reaches the map through a write that touches only `contracts`, with the byte-identity precondition for a canonical artifact — `tests/shell-project-map-contracts.test.ts` (one field, other capabilities, varied metadata, and the normalization of a non-canonical artifact).
    - A draft map is refused and an invalid artifact is never written — the same file (draft, unknown capability, invalid candidate, missing and corrupt artifact, unwritable directory).
    - The projection reports lead, satellites, five conflicts, derived readiness and completion, a next safe action, writes nothing, and reports corrupt state as unknown — `tests/project-map-coordination-state.test.ts` (fourteen fixtures, including the byte-identical read-only proof and the unusable-instant refusal).
    - `dependency-ready` and `completion` are derived and no journal duplicates the store — the same file, plus the absence of any journal module in the diff.
    - No runtime field enters the versioned map and nothing here grants delivery authority — `tests/shell-project-map-schema.test.ts` pins the forbidden-field list, and `applyProjectMapContract` writes only into `contracts`.
    - The transport stays a signal path — no coordination fact in PM-5 is delivered by a notification; every fact is a store record, and no new transport was added.
  - **What was not verified by an independent audit**: PM5-4's final state and this documentation slice. PM5-1, PM5-2 and PM5-3 each had one, and PM5-2 had two. If a later reader wants the same treatment for PM5-4, it is one audit away, and the honest reason it did not happen is that the parent's turn ended first.
  - Gates at the close of the unit: the project-map family 341/341, the full suite 3743 / 3705 pass / 0 fail / 38 skipped, and the type gate with 195 recorded diagnostics and no regressions — all three read by exit status, not by the tail of their output.

## Acceptance criteria

- A shared-contract proposal is a canonical durable record with a digest of its body, and a decided proposal is never overwritten.
- A second decision on the same contract is refused and the first decision survives byte-for-byte.
- The lead is a claim on a reserved capability id: a second lead is refused while a lease is live, and a stale lead is recovered with a visible warning.
- Only the session holding the lead claim can accept or reject a contract, and that check lives in the command and the projection, never in the store.
- An accepted contract reaches the versioned map through a write that touches only that capability's `contracts` array; for an artifact that is already canonical — the only kind the authorized writers produce — the approval block, capabilities, surfaces, dependencies and foundations are byte-identical before and after, and a valid but non-canonical artifact is normalized by the same write.
- A draft map is refused, and the artifact is never written when validation fails.
- The coordination projection reports lead, satellites, the five detectable conflicts, dependency readiness, completion and a next safe action, writes nothing, and reports corrupt state as unknown instead of empty.
- `dependency-ready` and `completion` are derived from the map and the receipts; no journal duplicates the store.
- No runtime field ever enters the versioned map, and nothing in PM-5 grants commit, push, PR, merge or release authority.
- The transport stays a signal path: no coordination fact exists only in a notification.

## Review workload forecast

Estimates: PM5-1 ≈ 350 lines (the store module plus its tests, the new record kind and three codes), PM5-2 ≈ 220, PM5-3 ≈ 350, PM5-4 ≈ 300, PM5-5 ≈ 150. Every slice stays under the 400-line review budget, and each is delivered as its own commit and its own native review. If a slice measures over budget, it is split before it is committed rather than after.

## Progress
- 2026-09-25: **PM-5 is complete.** PM5-5 closed the unit: the coordination protocol is documented in `docs/project-map.md` (the records, the lead and its satellites, the propose-decide-apply loop for shared contracts, what a satellite may and may not do, the five conflicts the projection reports, and the command usage) and in `docs/gentle-shell.md`; the card's stale `Runtime overlay: unavailable until PM-4.` became `Runtime overlay: not wired yet.`, which is what is actually true now; and every acceptance criterion was traced to the test that pins it. The unit is five slices, all delivered and verified by the parent while RDD was off, and the deferred review pass over those five parts is the next action.
- 2026-09-25: PM5-4 delivered and verified by the parent, with no native review because RDD is off by the user's decision. The command surface now carries `lead claim|renew|release|status` and `contract propose|accept|reject|list`: the session identity comes from the command's own session manager, the body digest is computed from the file, only a live lead may decide, and an acceptance records the durable decision before it touches the artifact, so a failed apply leaves the decision intact and retryable. PM5-5 (documentation and verification of the unit) is the last slice.
- 2026-09-25: PM5-3 delivered and verified by the parent, with no native review because RDD is off by the user's decision. The projection reports the lead, the satellites, the five detectable conflicts, derived dependency readiness and completion, and a next safe action per capability; it writes nothing, and it now refuses the time-dependent parts when the caller's instant cannot be parsed instead of reporting every live lease as stale. PM5-4 (the command surface) is next.
- 2026-09-25: PM5-1 and PM5-2 delivered and verified by the parent, with no native review because RDD is off by the user's decision. Shared-contract proposals are durable store records with a decision that can never be overwritten, and an accepted contract reaches the versioned map through one bounded writer that touches only that capability's `contracts` array. The independent audit of PM5-1 found no production defect and six test-quality gaps, all closed; PM5-2's probes left two survivors that are provably behaviour-preserving defence-in-depth layers. PM5-3 (the coordination projection) is next.
- 2026-09-25: unit planned after a read-only surface map (what PM-4 already gives, what the versioned map does and does not model — `contracts` is still an unresolved `string[]` — the PM-2 approval lifecycle and its single authorized writer, the two notification transports and their zero durability, and the card's hardcoded `Runtime overlay: unavailable`). The user settled the four open product decisions and, in a follow-up, scoped the lead's direct write to the `contracts` array of an already approved map. Nothing has been implemented yet; PM5-1 is the next work unit.

## Next decision

PM-5 is complete: PM5-1 through PM5-5 are delivered. The next action is the deferred review pass, the same shape PM-4's took: switch RDD on globally (`gentle-ai review mode enable --scope global`, because the clone-local override can only disable), review each of the five parts as its own candidate from a `git worktree` pinned at that part's tip with `baseRef` set to the previous part's tip, then return the switch to off with `gentle-ai review mode disable --scope global`. After the pass, the roadmap continues with PM-6 through PM-9, which need their own planning; PM-6 is the natural next unit because the coordination projection deliberately stops short of file-level surface enforcement until worktrees are bound to capabilities.

One consequence of the user's decision is worth stating where a later reader will find it: the lead now has a write path into the versioned artifact, scoped to `contracts`. If that ever needs to be narrowed or widened, the place to change is `applyProjectMapContract` and the acceptance criteria above, and the change is a product decision rather than a bug fix.
